//const store = await StoreModel.findById(id).populate("userId");

// const store = await StoreModel.create({ userId, ...storeData });
// await UserModel.findByIdAndUpdate(userId, { storeId: store._id });
