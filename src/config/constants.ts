export const CookiesKeys = {
  refreshToken: "refresh_token",
};

export const URL_ORIGIN = process.env.URL_ORIGIN || "http://localhost:5173";

export const ORIGIN = ["http://localhost:5173", URL_ORIGIN]
